<UserControl
    x:Class="MasteriesQuest.Controls.SummonerSearchBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MasteriesQuest.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Horizontal">
        <ComboBox x:Name="ServeComboBox" ItemsSource="{Binding Servers}" SelectedItem="{Binding Server}" DisplayMemberPath="Abbreviation" FontSize="36" Background="#00007d" />
        <AutoSuggestBox x:Name="SummonerNameAutoSuggestBox" Text="{Binding SummonerName}" PlaceholderText="Search for summoner..." FontSize="36" Width="500" Background="#00007d" />
    </StackPanel>
</UserControl>
